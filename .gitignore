node_modules
/dist
dist-server
.env
*.log
*.tar.gz
# The interface is embedded in the binary, so its build output lives beside the
# server. Nothing of it is tracked, not even the directory: a server built
# without an interface is not a thing worth producing, and go:embed refusing to
# find it is what says so.
src/server/dist
