{
  "compilerOptions": {
    "target": "ES2020",
    "lib": [
      "ES2020"
    ],
    "module": "CommonJS",
    "moduleResolution": "node",
    "outDir": "dist-server",
    // rootDir is the server folder, not the repo, so the build keeps landing on
    // dist-server/server.js — the path the service units and scripts expect.
    "rootDir": "server",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true
  },
  "include": [
    "server"
  ]
}
